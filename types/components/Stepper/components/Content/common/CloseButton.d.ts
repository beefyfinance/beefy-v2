/** Close the stepper without clearing inputs/quotes (e.g. on error, user can retry) */
export declare const CloseButton: (() => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};
/** Close the stepper AND clear inputs/quotes (after a successful tx) */
export declare const CloseAndResetButton: (() => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};

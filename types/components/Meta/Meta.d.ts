export type MetaProps = {
    title?: string;
    description?: string;
    noindex?: boolean;
    disableTitleTemplate?: boolean;
};
export declare const Meta: (({ title, description, noindex, disableTitleTemplate, }: MetaProps) => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};

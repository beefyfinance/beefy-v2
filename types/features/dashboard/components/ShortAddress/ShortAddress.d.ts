export type ShortAddressProps = {
    address: string;
    addressLabel?: string;
};
export declare const ShortAddress: (({ address, addressLabel, }: ShortAddressProps) => import("react/jsx-runtime").JSX.Element | null) & {
    displayName?: string;
};

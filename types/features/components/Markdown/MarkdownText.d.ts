import { type Components } from 'react-markdown';
export declare const MarkdownText: (({ text, className, components, }: {
    text: string;
    className?: string;
    components?: Components;
}) => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};

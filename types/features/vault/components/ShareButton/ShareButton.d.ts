import type { ShareButtonProps } from './types';
export declare const ShareButton: (({ vaultId, placement, mobileAlternative, hideText, }: ShareButtonProps) => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};

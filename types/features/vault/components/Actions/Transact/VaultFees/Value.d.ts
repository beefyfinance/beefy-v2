import type { ReactNode } from 'react';
export type ValueProps = {
    children: ReactNode;
};
export declare const Value: (({ children }: ValueProps) => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};

import { type PropsWithChildren } from 'react';
export type GraphLoaderProps = PropsWithChildren<{
    imgHeight?: number;
}>;
export declare const GraphLoader: (({ imgHeight, children, }: GraphLoaderProps) => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};

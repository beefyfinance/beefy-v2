interface GraphProps {
    vaultId: string;
    period: number;
    address?: string;
}
export declare const Graph: (({ vaultId, period, address }: GraphProps) => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};
export {};

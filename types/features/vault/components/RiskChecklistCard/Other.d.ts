import { type ReactNode } from 'react';
type OtherProps = {
    title: string;
    children: ReactNode;
};
export declare const Other: (({ title, children }: OtherProps) => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};
export {};

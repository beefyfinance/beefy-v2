export type PartnerCardProps = {
    logo?: string;
    title: string;
    content: string;
    url: string;
};
export declare const PartnerCard: (({ logo, title, content, url, }: PartnerCardProps) => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};

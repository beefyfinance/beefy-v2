import { type VaultEntity } from '../../../data/entities/vault';
interface PlatformsCardProps {
    vaultId: VaultEntity['id'];
}
export declare const PlatformsCard: (({ vaultId }: PlatformsCardProps) => import("react/jsx-runtime").JSX.Element) & {
    displayName?: string;
};
export {};

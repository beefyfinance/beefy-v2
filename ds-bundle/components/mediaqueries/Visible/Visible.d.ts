import * as React from 'react';

/**
 * Visible — from beefy-v2@0.1.0.
 */
export interface VisibleProps {
  children: React.ReactNode;
  else?: React.ReactNode;
}

export declare const Visible: React.ComponentType<VisibleProps>;

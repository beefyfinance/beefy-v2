VisibleAbove from beefy-v2. Use via `window.BeefyV2.VisibleAbove` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<BeefySurface>` (full provider chain in README.md — components read theme/i18n from that context).

@dev use <Visible/> or <Hidden/> if matching a breakdown

## Props

```ts
interface VisibleAboveProps {
  width: number;
  children?: React.ReactNode;
}
```

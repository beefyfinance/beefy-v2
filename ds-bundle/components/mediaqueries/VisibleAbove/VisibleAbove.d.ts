import * as React from 'react';

/**
 * VisibleAbove — from beefy-v2@0.1.0.
 */
export interface VisibleAboveProps {
  width: number;
  children?: React.ReactNode;
}

export declare const VisibleAbove: React.ComponentType<VisibleAboveProps>;

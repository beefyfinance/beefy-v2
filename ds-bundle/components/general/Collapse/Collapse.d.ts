import * as React from 'react';

/**
 * Collapse — from beefy-v2@0.1.0.
 */
export interface CollapseProps {
  in: boolean;
  children: React.ReactNode;
}

export declare const Collapse: React.ComponentType<CollapseProps>;

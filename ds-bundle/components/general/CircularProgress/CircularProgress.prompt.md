CircularProgress from beefy-v2. Use via `window.BeefyV2.CircularProgress` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<BeefySurface>` (full provider chain in README.md — components read theme/i18n from that context).

## Props

```ts
interface CircularProgressProps {
  size?: number;
  className?: string;
}
```

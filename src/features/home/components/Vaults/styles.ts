import { css } from '@repo/styles/css';

export const styles = {
  vaults: css.raw({
    marginTop: '32px',
    borderRadius: '12px',
    border: 'solid 2px {colors.background.content.dark}',
  }),
};

import { css } from '@repo/styles/css';

export const styles = {
  holder: css.raw({
    textStyle: 'subline.sm',
    color: 'text.middle',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  }),
};

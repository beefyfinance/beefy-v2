import { css } from '@repo/styles/css';

export const styles = {
  container: css.raw({
    display: 'flex',
    width: '100%',
  }),
  arrow: css.raw({
    color: 'text.dark',
    height: '24px',
  }),
};

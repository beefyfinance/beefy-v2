import { Theme } from '@material-ui/core/styles';

export const styles = (theme: Theme) => ({
  fiatAdornment: {
    display: 'flex',
    alignItems: 'center',
    color: '#F5F5FF',
  },
  flag: {
    marginRight: '8px',
  },
});

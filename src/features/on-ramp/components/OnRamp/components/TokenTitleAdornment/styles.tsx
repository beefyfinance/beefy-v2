import { Theme } from '@material-ui/core/styles';

export const styles = (theme: Theme) => ({
  tokenAdornment: {
    display: 'flex',
    alignItems: 'center',
    color: '#F5F5FF',
  },
  icon: {
    marginRight: '8px',
  },
});
